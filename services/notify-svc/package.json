{
  "name": "@epl/notify-svc",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "nest build",
    "dev": "nest start --watch",
    "start": "node dist/main.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"(no tests yet for notify-svc)\" && exit 0"
  },
  "dependencies": {
    "@epl/config": "workspace:*",
    "@epl/contracts": "workspace:*",
    "@epl/observability": "workspace:*",
    "@nestjs/common": "^10.4.1",
    "@nestjs/core": "^10.4.1",
    "@nestjs/platform-express": "^10.4.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@nestjs/cli": "^10.4.4",
    "@types/node": "^20.19.42",
    "typescript": "^5.5.4"
  }
}
